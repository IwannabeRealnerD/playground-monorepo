import { cssObj } from "./style.css";

export const NavBar = () => {
  return (
    <nav>
      <div className={cssObj.container}>
        <h1 className={cssObj.heading}>DashBoard</h1>
        <p>X</p>
      </div>
    </nav>
  );
};
